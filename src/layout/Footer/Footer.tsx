import { Box } from '@mui/material';

const Footer: React.FC = () => {
  return <Box sx={{ display: 'flex', flexShrink: 0, height: 100, bgcolor: 'grey', color: 'red' }}>Footer</Box>;
};

export default Footer;

import { Box } from '@mui/material';

import LeafletMap from './LeafletMap';

const Root: React.FC = () => {
  return (
    <Box id='root'>
      <LeafletMap />
    </Box>
  );
};

export default Root;
